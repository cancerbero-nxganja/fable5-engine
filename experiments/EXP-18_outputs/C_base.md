# Condition C — Sonnet base, NO skill (neutral framing) · 38183 tokens, 6 tool_uses

**Go/No-Go note for Finance — mid-cycle plan-change proration (ship date: Monday)**

**No-go.** The 7/7 green test suite is not evidence this is safe to enable — the tests assert the current code's outputs, and the code diverges from your written policy in ways that mean it does not compute the numbers Finance signed off on. Concretely: (1) the old-plan credit is calculated from today's published price (`PLANS[sub.plan]` = $50 for Starter) instead of the price the customer actually paid (`sub.paid_price` = $40), which the field exists to carry but the code never reads — a direct violation of rule 2; (2) the proration window is hardcoded to a 30-day cycle (`DAYS_IN_CYCLE = 30`) rather than the customer's actual cycle length (31 days for Acme), violating rule 1; (3) the credit is *added* into `net` instead of subtracted, so it inflates rather than reduces the amount owed, and discount/tax are computed on the pre-credit new-plan charge rather than the net-of-credit amount — a `taxed_base` variable computing the correct net is even present in the code but silently discarded — violating rule 3; and (4) most seriously, the persisted invoice line items sum to $40.92 while the customer is actually charged $100.92, a $60 discrepancy that directly breaks rule 4's requirement that lines sum to the amount charged. Re-running the signed-off Acme worked example under your actual policy (31-day cycle, $40 paid price, credit subtracted before discount/tax) yields a correct total of **$47.88**, versus the **$100.92** the current engine would charge and the **$40.92** its own ledger lines would show — three different numbers for one invoice. This needs a code fix plus a regression suite rewritten against the written policy (not against the current buggy behavior) before it can go live; recommend holding Monday's release.
