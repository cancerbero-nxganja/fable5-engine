# Condition B_full — Sonnet + full skill v1.17 (595 lines), neutral framing · 98991 tokens, 13 tool_uses

No-go. I independently recomputed the signed-off Acme reference invoice straight from Finance's four written rules (31-day actual cycle, the $40/mo Starter price Acme was actually paying, credit subtracted before discount, discount applied to the net subscription amount, tax applied to the discounted net, rounding once) and got a correct total of **$47.88** — but `billing.py` returns **$100.92**, and worse, that $100.92 doesn't even reconcile with its own persisted invoice lines, which sum to **$40.92** (verified by running the code: `sum(lines) != total` in every case with a nonzero credit — e.g. the existing downgrade test returns `total=$143.40` against a line-item sum of **-$56.60**), a direct violation of Finance's rule 4 that lines must sum to the amount charged. The root cause is `net = new_charge + old_credit` in `mid_cycle_change`, which *adds* the old-plan credit instead of subtracting it (violating rule 2's "credit reduces what they owe"), compounded by three more bugs: the credit is priced off today's published list price (`PLANS["starter"]` = $50) rather than the `paid_price` Acme actually pays ($40) — that field is declared on `Subscription` but never read anywhere in the module, so every credit uses the wrong, non-as-of price; `DAYS_IN_CYCLE` is hardcoded to 30 with no parameter for an account's actual cycle length (31 for Acme), violating rule 1; and the discount and tax are both computed off the raw new-plan charge rather than the net-of-credit, post-discount base rule 3 specifies. The 7/7 green tests don't clear any of this — their expected values (`old_credit=30.0`, `new_charge=72.0`, `total=100.92`) were reverse-engineered from this same 30-day/list-price/inverted-sign code rather than from Finance's independently written policy, so the suite is a frozen snapshot of the bug, not an oracle against it. Recommendation: do not ship Monday — fix the credit sign/aggregation, use `paid_price` as the as-of source for the old plan, thread through the account's actual cycle length, rebase discount/tax on the net-of-credit amount per rule 3, and rebuild the test suite's expected values independently from Finance's written rules (not from the code's output) before re-requesting sign-off.
